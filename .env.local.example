# App URL — must match your deployment URL exactly (used for OAuth callback)
# Local dev: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=

# Lock app to a single GitHub account (your username)
# Anyone else who tries to log in sees a "deploy your own" message
# Leave blank to allow any GitHub account (not recommended for production)
ALLOWED_USER=

# GitHub OAuth App credentials
# Create at: github.com/settings/developers → OAuth Apps → New OAuth App
# Callback URL: https://your-app.vercel.app/api/auth/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# GitHub Webhook Secret
# Set this to any random string, then use the same value when configuring
# webhooks in your GitHub repo settings
GITHUB_WEBHOOK_SECRET=

# Session encryption (32+ character random string)
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# Web Push VAPID keys (for push notifications)
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Upstash Redis — persists push subscriptions across serverless restarts
# Free tier at https://upstash.com (no credit card required)
# Create a Redis database → copy the REST URL and token from the dashboard
KV_REST_API_URL=
KV_REST_API_TOKEN=
